{
  "name": "laba_2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "eslint . --ignore-path .gitignore",
    "lint:fix": "eslint . -fix --ignore-path .gitignore",
    "format:fix": "prettier --write . --ignore-path .gitignore",
    "format": "prettier --check . --ignore-path .gitignore",
    "slint": "stylelint \"**/*.css\" --ignore-path .gitignore",
    "slint:fix": "stylelint \"**/*.css\" --fix --ignore-path .gitignore"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "2.4.1",
    "stylelint": "^13.13.1",
    "stylelint-config-standard": "^22.0.0"
  }
}
